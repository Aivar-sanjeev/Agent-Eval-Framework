# NVIDIA NIM — https://build.nvidia.com (OpenAI-compatible chat completions)
NVIDIA_API_KEY=

# Optional overrides
# NVIDIA_BASE_URL=https://integrate.api.nvidia.com/v1
# Same family works well for agent + judge; swap models independently per workload.
# NVIDIA_AGENT_MODEL=meta/llama-3.3-70b-instruct
# NVIDIA_JUDGE_MODEL=meta/llama-3.3-70b-instruct
